# Runtime deps for the SERVING image only (Streamlit demo + FastAPI).
# The demo surfaces read the pre-computed scored-edge artifact — they do not
# train anything, so torch / torch-geometric / lightgbm / jupyter stay out and
# the image is an order of magnitude smaller. Full training deps live in
# requirements.txt.
numpy>=1.26,<2.1
polars>=1.0
pyarrow>=15.0
networkx>=3.2
matplotlib>=3.8
streamlit>=1.33
fastapi>=0.110
uvicorn>=0.29
anthropic>=0.39
